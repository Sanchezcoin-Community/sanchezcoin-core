// Wird verwendet um eine Datei an einen Ordnerpath zu hngen
function os_based_file_path_create(file_name, path) {
    if(process.platform === "win32") {
        if(path.endsWith() === true) {

        }
        else {
            
        }
    }
    else {
        if(path.endsWith() === true) {

        }
        else {
            
        }
    }
};


module.exports = {
    os_based_file_path_create:os_based_file_path_create
}