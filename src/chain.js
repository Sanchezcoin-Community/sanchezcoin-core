class ProofOfWorkBlockchain {
    constructor(genesis_block, target, hash_algo, coin, options, node_config) {

    }

    // Wird verwendet um die Blockchain Datenbank zu laden
    loadBlockchainDatabase(callback) {

    }
}


module.exports = {
    ProofOfWorkBlockchain:ProofOfWorkBlockchain
}