async function mine_block_hash(hash_algo, block_header, diff) {

};

async function verify_pow(block_header, nonce, diff, hash_algo) {

};

// Exportiert die Funktionen
module.exports = { mine_block_hash, verify_pow }